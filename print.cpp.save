#include "print.h"

void print( char text[DEFAULT_STRING_LENGTHS][DEFAULT_NUMBER_OF_RAWS] )
{
    for ( int i = 0; i < DEFAULT_STRING_LENGTHS; i++ )
    {
            for ( int j = 0; j < DEFAULT_NUMBER_OF_RAWS; j++ )
            {
                printf( "%c", text[i][j] );
            }
    }
}
